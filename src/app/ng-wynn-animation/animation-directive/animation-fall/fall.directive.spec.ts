import { FallDirective } from './fall.directive';

describe('FallDirective', () => {
  it('should create an instance', () => {
    const directive = new FallDirective();
    expect(directive).toBeTruthy();
  });
});
