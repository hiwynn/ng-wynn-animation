import { WidenDirective } from './widen.directive';

describe('WidenDirective', () => {
  it('should create an instance', () => {
    const directive = new WidenDirective();
    expect(directive).toBeTruthy();
  });
});
