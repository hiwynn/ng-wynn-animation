import { WaveDirective } from './wave.directive';

describe('WaveDirective', () => {
  it('should create an instance', () => {
    const directive = new WaveDirective();
    expect(directive).toBeTruthy();
  });
});
